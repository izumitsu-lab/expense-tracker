# 家計簿アプリ v2.3 修正内容（v2.2 からの追加分）

## ⚠️ アップロード時の注意

- 次の5ファイルを置き換えてください：`index.html`、`app.js`、`styles.css`、`tailwind.css`、`sw.js`。
  - `vendor/Sortable.min.js` と `manifest.webmanifest` は v2.2 から変わっていません。
- **サーバー（Firestore）の設定変更は不要です。** 取引・定期支出・クイック入力に `shopId` という欄が増えるだけです。
- 更新後に初めて起動すると、既存の記録に `shopId` が自動で付きます。
  - 付いた結果は通常の同期でクラウドへ送られます。
  - 件数が多いと、初回だけ同期に数秒かかります。
- **iPhone・Mac の両方を v2.3 に揃えてください。**
  - v2.2 の端末で記録を編集しても壊れません。v2.3 の端末が次に起動したときに、自動で `shopId` を付け直します。

## ✅ 追加・修正

| 内容 | 詳細 |
|---|---|
| お店を `shopId` で参照（点検 #9） | 記録・定期支出・クイック入力が、お店をIDで参照するようにしました。<br>・`shopName` も残すため、表示と互換性はそのままです。<br>・既存データは「店名＋分類」から自動でIDを付けます。<br>・同じ名前のお店が複数あり、分類でも絞れない記録は、従来どおり名前だけで扱います。 |
| お店の名前変更 | IDで紐付いた記録は、名前を変えても確実に追従します。 |
| 同じ名前のお店を区別 | 分析の Shop タブで、分類の違う同名のお店を別々に集計します。<br>Monthly・Yearly の「お店別」では、名前が重なるときだけ「駅前店（カフェ）」のように分類名を添えます。 |
| 削除したお店の記録 | 記録時の名前とIDを保ったまま残ります。 |
| 支払い方法の自動更新 | 「最後に使った支払い方法を既定にする」も、お店IDで照合するようにしました。 |
| **削除の取り消し（Undo）** | 削除の直後、画面下に「〇〇を削除しました ／ 元に戻す」を約7秒表示します。<br>・対象：記録、定期支出、お店、分類、カテゴリー、支払い方法、クイック入力、時間帯、クイック時刻<br>・元に戻すと、同じIDで元の位置に戻ります。クラウドにも再保存されます。<br>・削除前の確認ダイアログは従来どおり出ます。 |
| 直近7日の1日あたり（点検 #16） | Trend の「今週の1日あたり」を「直近7日の1日あたり」に変えました。比較対象は、その前の8週間です。<br>これで、日曜に1日分だけで計算されて数字が大きく揺れることがなくなります。<br>支出の推移グラフ（週ごと）は従来どおり、日曜始まりの週です。 |
| 金額未定の注記（点検 #17） | 金額未定の記録が期間内にあるとき、赤字で「※ 金額未定の記録 N件 は含まれていません」と表示します。<br>表示場所：Monthly・Yearly の合計の下、Trend の着地予測の下 |

## ⏸ 引き続き未対応

| 内容 | 理由 |
|---|---|
| `authDomain` の Safari 対策（点検 #20） | アプリをどこで公開しているか（Firebase Hosting か、それ以外か）で手順が変わります。コードだけでは対応できません。<br>公開先を教えてもらえれば手順を案内します。 |
| ボタンの固定処理の onclick の全廃・コード分割 | データを埋め込む危険な箇所は v2.2 で対応済みです。残りは固定の処理だけで、安全上の問題はありません。<br>大規模に書き換えると挙動が変わる恐れがあるため、見送りました。 |
| 古い保存キー（v15〜v17）の削除 | 万一のときの復旧元になるため、残しています。 |

## 検証内容（v2.3）

- **見た目：** 12画面を v2.2 以前と比較しました。変わったのは次の2か所だけでした。
  - 月送りの修正
  - Trend の「直近7日」の表示
  - （削除時に出る「元に戻す」の表示は新しく追加したものです）
- **同期：** 模擬 Firestore の8つのシナリオをすべて再実行し、v2.2 と同じ結果になりました。
- **新機能：** 次の動作を確認しました。
  - 既存データへの `shopId` 自動付与：同名店は分類で区別し、閉店した店は名前のまま残る
  - Shop タブでの同名店の別集計
  - 名前変更の追従
  - 記録削除の取り消しで、元の位置に戻ること
  - 分類削除の取り消し
  - トーストが7秒後に消えること
  - 金額未定の注記

---

# 〔参考〕v2.2（点検修正版）修正内容

対象：点検レポートの指摘事項のうち、下記「✅ 修正済み」のもの。
デザイン・画面構成・操作手順は変更していません（修正前後の画面をピクセル単位で比較し、変わったのは月送りバグの修正箇所だけでした）。

## ⚠️ アップロード時の注意

- **ファイル一式をすべて置き換えてください。**
  - 既存：`index.html` `app.js` `styles.css`
  - 新規：`tailwind.css` `vendor/Sortable.min.js` `sw.js` `manifest.webmanifest`
- `favicon-32.png` と `apple-touch-icon.png` は、今あるものをそのまま使います。
- **Firestore のデータ構造・コレクション名・セキュリティルールは変更不要です。**
  旧版と新版の端末が同じアカウントで混在しても同期できます。
  ただし、削除の復活防止などの効果は新版の端末にだけ働くため、全端末の更新をおすすめします。
- 更新後に初めて開いたときは、以前と同じ動作（和集合マージ）で1回同期します。
  その時点から「同期済みIDの記録」が始まり、2回目以降は削除した記録が復活しなくなります。

## ✅ 修正済み

### 1. 同期の安全化（データ消失・混入対策）

| # | 内容 | 修正方法 |
|---|---|---|
| 1 | 他の端末で削除した記録が復活する | 端末ごとに「クラウドに存在を確認したID」を記録します（端末内のみ、サーバーには保存しません）。以前クラウドにあって今は無い項目は削除済みと判断し、再アップロードしません。オフライン起動時やクラウドが丸ごと空のときは、安全側に倒して削除判定をしません。 |
| 1 | 定期支出が他端末で再生成される | 最終実行日を各定期支出の `lastRunDate` 欄にも保存します（既存の fixedExpenses コレクションの中の欄なので、ルール変更は不要です）。また、自動追加はクラウドの最新状態を読み込んでから実行します。 |
| 2 | 別アカウントにデータが混入する | 前回と違うアカウントでログインした場合、前のアカウントで同期済みのデータは持ち込みません。ログアウト中に新しく入力した分だけを引き継ぎます。新しいアカウントで設定が空のときは初期設定を入れます。 |
| 3 | 500件を超えると同期が失敗する | 400件ずつに分割して送信します（1,200件でテスト済み）。 |
| 4 | 送信待ちの編集が、他端末からの更新で消える | 未送信の変更がある間に更新が届いた場合は、自分が触った項目はローカルの内容、触っていない項目はリモートの内容を採用して統合します。 |
| — | ログアウト後もリアルタイム監視が残る | ログアウト時に監視を解除し、同期状態をリセットします。二重ログイン時の二重登録も防止しました。 |
| — | `enablePersistence` の失敗を捕まえられていない | Promise の `.catch` で処理するようにしました。 |
| — | 復元直後のリロードで送信が途中で切れる | クラウドへの送信完了を待ってから再読み込みします。 |
| — | アプリを閉じる直前の変更が送られない | 画面が裏に回るときに、未送信の変更をすぐ送ります。 |

### 2. ロジックの不具合

| # | 内容 |
|---|---|
| 7 | 月送り・年送りで月が飛ぶ（8/31→10/1）。月末日に丸めるようにしました（履歴・分析の両方）。 |
| 8 | 既存の記録を編集中に日付を変えても、時刻が消えないようにしました。新規入力時の「時間を選び直す」動作は従来どおりです。 |
| 9 | お店の名前を変えると、過去の記録・定期支出・クイック入力のお店名も追従するようにしました。同じ名前のお店が他の分類にある場合は、分類も一致するものだけを変えます。「最後に使った支払い方法を既定にする」も分類まで照合します。 |
| 10 | 分類を削除すると、所属していたお店がお店リストの「未分類」欄に表示されるようにしました。そこから編集・削除したり、別の分類へドラッグしたりできます。使用中のカテゴリー・支払い方法を削除するときは、影響件数を表示します。 |
| 11 | 定期支出を「毎月の追加日」と同じ日に登録すると、今日の分も追加されるようにしました。アプリを開いたまま日付をまたいだ場合も処理されます。 |
| 12 | 一覧の⊖ボタンから時間帯・クイック時刻を最後の1件まで消せないようにしました。 |
| 13 | CSV の修正です。時間未設定の記録を「00:00」と出力しないようにしました。ファイル名は日本時間の日付にしました。末尾に「費目タイプ」（固定費/変動費）列を追加しました。 |
| 14 | 日付文字列をローカル日付として解釈するようにしました。海外のタイムゾーンでも曜日や週がずれません。 |
| — | 「金額だけ保存」の後、履歴画面を必ず今日の日付で表示するようにしました。 |

### 3. 分析

| # | 内容 |
|---|---|
| 15 | 「今月の着地予測」と「カテゴリー別の変化」で、固定費を日割りで引き伸ばさないようにしました。固定費は実額で計上し、今月まだ追加されていない定期支出は予定額として加えます。日割りで伸ばすのは変動費だけです。月初に家賃が入っても、予測が数百万円に跳ね上がりません。 |

### 4. セキュリティ・オフライン

| # | 内容 |
|---|---|
| 18 | 「すべて見る」ボタンにデータを埋め込むのをやめました。`McDonald's` のように `'` を含む店名でも動作します。 |
| 19 | 設定一覧（お店・分類・カテゴリー・支払い方法・時間帯・クイック入力）で、IDを onclick 文字列に埋め込まず、イベントリスナーで処理するようにしました。時刻や日数も表示時にエスケープします。 |
| 19 | JSON 復元時の検証を強化しました。全項目のID（Firestore で使えない `/` などの禁止）、IDの重複、日付・時刻・金額の形式を確認します。 |
| 21 | CSV で `= + - @` から始まる文字の先頭に `'` を付け、Excel で数式として実行されないようにしました。 |
| 6 | Tailwind を CDN の実行時生成から、同じ v3.4.1 でビルドした `tailwind.css` に変更しました（見た目は同一）。SortableJS も同梱しました。並び替えライブラリが読めない場合でも一覧は表示されます。 |
| 5/6 | Service Worker と manifest を追加しました。一度オンラインで開けば、電波の無い場所でも起動できます。アプリ本体は「ネット優先」で取得するため、ファイルを更新すれば次回起動時に反映されます。Firestore とログインの通信には関与しません。**iPhone では「ホーム画面に追加」して使うと、Safari の7日間データ自動削除の対象外になります。** |

### 5. UX・アクセシビリティ

- 詳細確認画面の「保存する」ボタンの下余白を、iPhone のホームインジケータ領域以上に確保するようにしました。ホームインジケータの無い機種では従来と同じ30pxです。
- アイコンだけのボタンに読み上げ用ラベル（`aria-label`）を付けました。

## ⏸ 今回は未対応（理由つき）

| # | 内容 | 理由 |
|---|---|---|
| 9 | お店を `shopId` で参照する方式への移行 | 旧版の端末と同期データの形式が変わるため見送りました。代わりに「名前変更の追従」で実害を解消しています。 |
| 16 | 今週の1日あたり（日曜は1日分だけで計算）の不安定さ | 表示の意味が変わる仕様変更になるため、見送りました。 |
| 17 | 金額未定の記録が含まれる期間の注記 | 画面デザインの追加が必要なため、見送りました。 |
| 20 | `authDomain` の Safari 対策 | Firebase Hosting 側の設定変更が必要なため、コードだけでは対応できません。 |
| — | 静的な onclick（ボタンの固定処理）の全廃・コード分割・テスト整備 | 挙動を変えずに大規模に書き換えることになるため、見送りました。データが埋め込まれる危険な箇所は修正済みです。 |
| — | 削除の取り消し（Undo） | 新機能の追加になるため、見送りました。 |
| — | 入力欄の自動ズーム・`user-scalable=no` | **点検レポートの訂正です。** 現在の `maximum-scale=1` が iOS のフォーカス時自動ズームを防いでいるため、変えるとかえってズームが起きます。現状維持にしました。 |
| — | 古い保存キー（v15〜v17）の削除 | 万一のときの復旧元になるため、残しています。 |

## 検証内容

- **見た目**：修正前後の12画面をピクセル比較しました（iPhone 相当の 390×844）。
  - 変わったのは、月送りバグの修正箇所の1か所だけでした。
- **同期**：模擬 Firestore で8つのシナリオを検証しました。
  - 対象：削除の復活、アカウント切替、1,200件、送信待ち中の競合、オフライン起動、定期支出の再生成、ログアウト後の監視、通常の編集・削除。
  - 旧版ではシナリオ1・2・3・4・6・7で不具合が再現し、新版ではすべて期待どおりに動きました。
- **画面操作**：以下が正しく動くことを確認しました。
  - 「すべて見る」（`'` を含む店名）
  - 日付変更後の時刻保持
  - 店名変更の追従
  - 分類削除後の未分類表示
  - 最低1件のチェック
  - 当日登録の定期支出
  - CSV 出力
  - 1/31→2月の月送り
- **本物の Firebase には接続していません。** 実機では、次の順で確認することをおすすめします。
  1. 更新前に JSON バックアップを取る
  2. 1台目を更新して同期を確認する
  3. 2台目を更新する
