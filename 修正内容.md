# 家計簿アプリ v2.8 修正内容（v2.7 からの追加分）

点検で残っていた低優先度の項目を、すべて対応しました。

| 対応した項目 | 内容 |
|---|---|
| 端末内の保存容量 | IndexedDB に移行 |
| 入力欄の文字サイズ | 16px 以上に |
| 画面の拡大 | ズームの禁止を解除 |
| 文字のコントラスト | 読みやすさの基準を満たすように |
| キーボード操作 | Tab・Enter・Esc・数字キーで操作できるように |
| PWA | Android 用のアイコンを追加、ダークモードに対応 |
| Service Worker | 正常な応答だけを保存 |
| コード | 分割と整理 |

## ⚠️ アップロード時の注意（ファイル構成が変わります）

- **新しく置くもの：**
  - `js` フォルダ（10ファイル）
  - `icon-192.png`
  - `icon-512.png`
  - `icon-maskable-512.png`
- **置き換えるもの：**
  - `index.html`
  - `styles.css`
  - `sw.js`
  - `manifest.webmanifest`
- **そのままのもの：**
  - `tailwind.css`
  - `vendor/Sortable.min.js`
  - `favicon-32.png`
  - `apple-touch-icon.png`
- **使わなくなるもの：** `app.js`（中身は `js` フォルダに移りました）。サーバーから消して構いません。残しても害はありません。
- `tests` フォルダはテスト用です。サーバーに置く必要はありません。
- **クラウド（Firestore）のデータの形式は v2.6・v2.7 と同じです。** 全端末を同時に更新する必要はありません。
  - v2.5 以前から更新する場合は、下の v2.6 の手順に従ってください。
- 初めて開いたときに、端末内のデータを localStorage から IndexedDB へ自動で移します。
  - 一瞬で終わり、操作は不要です。
  - 念のため、元のデータは localStorage に `premium_tracker_backup_before_idb` という名前で残します。

## ✅ 対応内容

### 1. 端末内の保存を IndexedDB に移行

| 内容 | 詳細 |
|---|---|
| 容量 | localStorage（約5MB）から IndexedDB に移しました。端末の空き容量に応じて、数百MB以上使えます。<br>実際の Chromium で確認した上限は約470MBでした（端末によって異なります）。 |
| 移行 | 初回起動時に、localStorage の記録と同期の記録を IndexedDB に移します。<br>・書き込めたことを確かめてから、元のデータをバックアップ名に移します。<br>・v2.7 でログインしていた端末は、更新後も差分同期のままです（全件を読み直しません）。 |
| 起動の順番 | 端末内のデータを読み終えてから、画面の表示と同期を始めます。空の画面が一瞬出ることや、読み込み前のデータで同期することはありません。 |
| 使えない環境 | IndexedDB が使えない環境（一部のプライベートブラウズなど）では、自動で従来の localStorage に保存します。 |
| 容量不足 | 保存に失敗したときは、画面に知らせます（従来と同じ）。 |
| 復元 | JSON の復元のあとは、端末内への保存が終わってから再読み込みします。 |

- **Safari の自動削除の扱いは従来と同じです。** ホーム画面に追加して使っていれば、7日間使わなくても消されません。

### 2. 入力欄の文字サイズと画面の拡大

- 画面の拡大の禁止（`maximum-scale=1`・`user-scalable=no`）を外しました。Android でもピンチ操作で拡大できます。
- iPhone では、16px 未満の入力欄に触れると画面が自動で拡大されます。これを防ぐため、すべての入力欄を16px 以上にしました（検索欄は13px → 16px）。
  - 検索欄の高さは従来のままです。
  - プレースホルダーは、途中で切れていた「キーワードで絞り込み」を「検索」にしました。
- ダブルタップでの拡大は止めました（数字ボタンを素早く押したときの誤動作を防ぐため）。ピンチ操作での拡大はできます。
- 2本指での操作を、横スワイプ（金額入力とクイック入力の切り替え、日付送り）と間違えないようにしました。

### 3. 文字のコントラスト

薄すぎた文字色だけを少し濃くしました。配色の雰囲気は変えていません。

| 色 | 変更前 | 変更後（白地でのコントラスト） |
|---|---|---|
| 補足の灰色 | #9CA3AF（2.5:1） | #6C6C70（5.2:1） |
| 説明の灰色 | #6B7280（4.8:1） | #636366（6.0:1） |
| 右寄せの値 | #8E8E93（3.3:1） | #6C6C70（5.2:1） |
| 青い文字 | #007AFF（4.0:1） | #0066D6（5.4:1） |
| 赤い文字 | #FF3B30（3.6:1） | #D70015（5.4:1） |
| 緑の文字 | #34C759（2.2:1） | #1E7B34（5.3:1） |

- 白い文字をのせる小さなボタンやバッジも、地の青・赤を少し濃くしました。グラフの棒などは従来の色です。
- **検査ツール（axe-core）の結果：** 12画面で181件あったコントラスト不足が **0件** になりました。追加の10画面を含む22画面、ライト・ダークの両方で0件です。

### 4. キーボード操作（Mac など）

| 操作 | できるようになったこと |
|---|---|
| Tab | 下部メニュー、分類、クイック入力、一覧の行、設定の行など、div で作られた部品も選べます。 |
| Enter・スペース | 選んだ部品を押せます。 |
| 開いていない画面 | 右から出る設定画面や、下から出る入力画面が閉じているときは、Tab で中に入りません。 |
| 開いている入力画面・ダイアログ | 開いている間は、その中だけを Tab で移動します。 |
| Esc | 一番手前の画面・ダイアログを閉じます（「キャンセル」と同じ動作）。 |
| 金額入力の画面 | 数字キー（テンキーを含む）、Backspace、C で入力できます。Enter で「お店を選ぶ」に進みます。 |
| 選択中の部品 | 青い枠を表示します。タッチ操作では出ません。 |

### 5. PWA のアイコンとダークモード

- **アイコン：** Android 用に、192px・512px・maskable（形を切り抜かれても欠けない版）を追加しました。元のアイコンと同じデザインで描き直しています。
- **ダークモード：** 端末の設定（外観モード）に合わせて、自動で切り替わります。
  - 色は iOS のダークモードに合わせました。
  - 文字はすべて、読みやすさの基準（4.5:1 以上）を満たしています。
  - 上部のバー（theme-color）や、日付・時刻の選択部品も暗い色になります。

### 6. Service Worker

- Firebase SDK の保存方法を変えました。
  - 以前は中身を確認できない形（`no-cors`）で保存していたため、エラーの応答でも保存・使用されることがありました。
  - 今は中身を確認できる形（CORS）で取得し、**正常な応答（200）だけを保存**します。
- 以前の版が保存した、中身を確認できない応答は使わずに取り直します。
- 分割した `js` フォルダと新しいアイコンも、オフライン用に保存します。
- 実際の Chromium で確かめたこと：
  - v2.7 では、エラー（500）の応答が保存されていました。
  - v2.8 では保存されず、あとで正常に取れたときに保存されます。

### 7. コードの分割と整理

1つの `app.js`（約4,000行）を、役割ごとに10ファイルに分けました。

| ファイル | 行数 | 内容 |
|---|---|---|
| `js/core.js` | 331 | 共通の関数・初期値・アプリの状態・お店の参照 |
| `js/storage.js` | 272 | 端末内の保存（IndexedDB）・定期支出の自動追加 |
| `js/ui.js` | 253 | ダイアログ・画面の切り替え・スワイプ・書き出しと復元 |
| `js/input.js` | 509 | 金額入力・分類とお店の選択・時間・詳細入力・削除の取り消し |
| `js/history.js` | 390 | 履歴・検索・絞り込み・並び替え |
| `js/stats.js` | 848 | 分析 |
| `js/settings.js` | 464 | 定期支出・設定の各画面 |
| `js/sync.js` | 804 | Firebase（ログイン・同期） |
| `js/keyboard.js` | 126 | キーボード操作 |
| `js/main.js` | 26 | 起動 |

- **重複の整理：**
  - 3つあった「変動費／固定費」の切り替え関数を1つにまとめました。
  - 週グラフと月グラフの描画コードを1つにまとめました。
  - 手で組み立てていた日付の文字列（4か所）を `toDateStr` に統一しました。
- **使われていないコードの削除：**
  - 存在しない選択肢 `'ADD_NEW'` の判定（5か所）
  - 「すべて見る」の古い文字列データの処理
  - 下部メニューの `data-sub` 関係の処理
- 読み込み時にすぐ実行する処理が、後のファイルの関数を使わないように順番を確かめました。

## 🧪 検証内容（v2.8）

**画面（実際の Chromium、iPhone と同じ 390×844）：**
- 22画面を v2.7 と比較しました。見た目が変わったのは、意図した次の2点だけでした。
  - 文字色の調整
  - 検索欄のプレースホルダー
- IndexedDB への移行と、コードの分割の前後では、22画面ともピクセル単位で同一でした。

**自動テスト（`tests` フォルダに同梱。すべて通過）：**

| テスト | 件数 | 結果 |
|---|---|---|
| `fixes.test.js`（点検 #6〜#10） | 13 | すべて通過 |
| `storage.test.js`（IndexedDB） | 8 | すべて通過 |
| `sync.test.js`（同期） | 20 | すべて通過 |
| `keyboard.browser.js`（実際のブラウザ） | 16 | すべて通過 |
| `sw.browser.js`（実際のブラウザ） | 7 | すべて通過 |
| `screens.browser.js`（22画面 × ライト・ダーク） | — | コントラスト不足0件、ページのエラー0件 |

- 同期のテストは、分割した10ファイルを本物のブラウザと同じ順番で `<script>` として読み込んで実行しています。
- **本物の Firebase・実機の iPhone では試していません。** 更新前に JSON バックアップを取り、1台目で次の点を確かめてから2台目を更新してください。
  - 表示
  - 同期
  - ダークモード

## ⏸ 残っていること

| 内容 | 説明 |
|---|---|
| ダークモードの手動切り替え | 今は端末の設定に合わせて自動で切り替わるだけです。アプリの中で選べるようにすることもできます。 |
| HTML の onclick 属性 | ボタンの固定の処理は、HTML の onclick 属性のままです。データを埋め込む危険な箇所は v2.2 で対応済みです。 |

---

# 〔参考〕v2.7 修正内容

点検の指摘 **#6〜#10** を直しました。自動テスト一式（`tests` フォルダ）も同梱しています。

## ⚠️ アップロード時の注意

- 置き換えるのは **`app.js` と `sw.js` の2ファイルだけ**です。
- **v2.6 からの更新なら、データの形式は変わりません。** 手順は通常の更新と同じです。
- **v2.5 以前から更新する場合**は、下の v2.6 の「更新手順」に従ってください（全端末の同時更新が必要です）。
- `tests` フォルダはテスト用です。公開する Web サーバーに置く必要はありません。

## ✅ 修正内容

| # | 内容 | 修正方法 |
|---|---|---|
| 6 | 過去の記録を編集すると、お店の既定の支払い方法が書き換わる | 「最後に使った支払い方法を既定にする」は、**新しく入力したときだけ**働くようにしました。過去の記録を直しても、お店の既定は変わりません。 |
| 7 | お店の紐付けで、別の分類の同じ名前のお店に紐付くことがある | 記録に分類があるときは、**同じ分類のお店にだけ**紐付けます。例えば、記録のお店が削除済みで、別の分類に同じ名前のお店が1件だけある場合、以前はそちらに紐付いていました。今は紐付けず、記録時の名前のまま残します。定期支出から記録を作るときも同じです。分類の無い古い記録は、従来どおり同じ名前のお店が1件だけなら紐付けます。 |
| 8 | 同じ分類に同じ名前のお店を作れてしまう | お店の登録・編集の両方で、**同じ分類に同じ名前のお店があると保存しない**ようにしました（名前の前後の空白は無視）。そのときは「〇〇には同じ名前のお店がすでにあります」と表示します。分類が違えば、同じ名前でも登録できます。 |
| 9 | 取り消し（Undo）が直前の1件だけ | 「元に戻す」が出ている間に続けて削除した分は、積み重ねて覚えておきます。表示は「2件を削除しました」のようになり、「元に戻す」で**まとめて元の位置に戻ります**。記録と定期支出など、種類が違っても一緒に戻せます。表示が消えた後の削除は、新しい取り消しとして扱います（最大20回分）。 |
| 10 | 月の途中から記録を始めた月は、着地予測が低く出る | 着地予測の日割りの起点を、「今月の1日あたり」と同じ**記録を始めた日**にそろえました。カテゴリー別の変化の予測も同じです。<br>・その月は「※ 記録を始めた 9/21 からの分で予測しています」と表示します。<br>・例：9/21 から1日3,000円ずつ記録した場合の予測は、修正前が ¥11,739、修正後は ¥30,000（9/21〜9/30 の10日分）です。<br>・前の月以前から記録している場合は、従来どおり月初から日割りします。 |

## 🧪 自動テストの同梱

`tests` フォルダに、今回までの確認で使ったテストをまとめました。
- Node.js があれば、`npm install` → `npm test` で実行できます（約1分）。
- 次に修正するとき、同じ確認をすぐにやり直せます。
- 詳しくは `tests/README.md` を見てください。

| ファイル | 件数 | 内容 |
|---|---|---|
| `fixes.test.js` | 13 | v2.7 の修正（#6〜#10） |
| `sync.test.js` | 20 | 差分同期・削除済みの印・移行・オフライン・自動復帰・スクリプト注入対策・authDomain |

## 検証内容（v2.7）

- **v2.7：** 同梱のテスト33件がすべて通りました。
- **v2.6 との比較：** `fixes.test.js` を v2.6 に当てると、13件のうち7件が失敗し、直した不具合が再現しました。
  - 失敗しなかった6件は、従来どおり動くべき動作を確かめるテストです。
- **本物の Firebase には接続していません。** v2.6 と同じく、1台目で確認してから2台目を更新してください。

## ⏸ 引き続き未対応（優先度：低）

| 内容 | 説明 |
|---|---|
| 端末内の保存容量 | 全データを localStorage（約5MB）に保存しています。長年使う場合は IndexedDB への移行を勧めます。 |
| ズームの禁止・入力欄の文字サイズ | 検索欄を16px以上にすれば、`maximum-scale=1` を外せます（Android でもピンチ操作のズームができるようになります）。 |
| キーボード操作・文字のコントラスト | 下部メニューなどが Tab キーで選べません。補足文字の灰色が薄く、読みやすさの基準を満たしていません。 |
| PWA のアイコン・ダークモード | Android 用のアイコン（192px・512px・maskable）がありません。ダークモードにも対応していません。 |
| Service Worker | Firebase の SDK を、中身を確認できない形（`no-cors`）で保存しています。 |
| コードの分割・重複の整理 | 1ファイルに約3,500行あります。重複しているコードと使われていないコードも残っています。 |

---

# 〔参考〕v2.6 修正内容（差分同期）

点検 #3（Firestore の読み取り量）を、**差分同期**で根本的に解決しました。
起動のたびに全件を読むのをやめ、**前回以降に変わった分だけ**を読みます。v2.4・v2.5 の修正はすべて含んでいます。

## ⚠️ 必ず読んでください（更新手順）

v2.6 はクラウド上のデータの形式を変えます。**v2.5 以前の端末とは正しく同期できません。**

1. **全端末で JSON バックアップを取る**（設定 → バックアップ）。
2. `app.js` と `sw.js` を置き換えてアップロードする（ほかのファイルは変更なし）。
3. **1台目**でアプリを開く。
   - 「正常に同期されています」と表示されるまで、閉じずに待ってください。
   - この1回だけ全件を読み、全データを新形式に書き直します。記録3,000件なら、書き込みも約3,000回です（1日の無料枠2万回の範囲内）。
4. **2台目以降**を開く（これも最初の1回だけ全件を読みます。書き直しは不要なので、書き込みはほぼありません）。
5. 古い版のまま開かないようにしてください。
   - 例：ホーム画面のアプリを電波のない場所で初めて開く、など。
   - 電波がある場所で一度開けば、新しい版に切り替わります。

**Firestore のセキュリティルール：** 各ドキュメントに2つの欄が加わります。`users/{uid}` 以下の読み書きを本人に許可するだけのルールなら、変更は不要です。
- `_updatedAt`：更新日時
- `_deleted`：削除済みの印

ルールで書き込める欄を個別に制限している場合は、この2つを許可してください。

## ✅ 新しい同期の仕組み

| 内容 | 詳細 |
|---|---|
| 変わった分だけ読む | 各ドキュメントに、サーバーの更新日時（`_updatedAt`）を付けます。各端末は「どこまで読んだか」を覚えておき、次の起動ではそれより新しいドキュメントだけを読みます。 |
| 削除は「削除済みの印」 | 削除するとき、ドキュメントを消すのではなく、`_deleted: true` の印を付けて残します。他の端末は「変わった分」を読むだけで、削除も受け取れます。<br>「元に戻す」（Undo）で同じIDを復元すると、印は上書きされます。 |
| ローカルの未送信の変更 | 端末内に「送信済みの内容の指紋」を保存します。アプリを閉じている間やオフライン中の変更（追加・編集・削除）も、次の起動時に確実に送ります。 |
| 同じ項目を両方の端末で編集した場合 | まだ送っていない、この端末の編集を優先して送ります（従来と同じ考え方です）。 |
| 30日ごとの全件読み込み | 前回の全件読み込みから30日たつと、1回だけ全件を読み直します。万一の取りこぼしを自然に直すための安全網です。 |
| 全件を読むとき | 次のときだけ全件を読みます。<br>・初めて v2.6 を開くとき<br>・別のアカウントでログインしたとき<br>・30日ごと<br>・オフラインで初回の読み込みが終わらなかったとき |

### 読み取り回数の目安

| 場面 | v2.4 | v2.5 | **v2.6** |
|---|---|---|---|
| 起動（他の端末で変更なし） | 全件×2 | 全件×1 | **0回** |
| 起動（他の端末で3件変更） | 全件×2 | 全件×1 | **3回** |
| 使用中に他の端末で1件変更 | 1回 | 1回 | 1回 |
| 30日に1回 | — | — | 全件×1 |

- 例：記録3,000件・1日10回起動・2台で使う場合
  - v2.4：1日 約12万回
  - v2.5：1日 約6万回
  - **v2.6：「その日に入力・編集した件数」の数倍程度**（毎日20件入力なら、おおむね100回未満）
- これに、30日ごとの全件読み込み（3,000件×台数）が加わります。
- 自分で保存した内容も、監視の更新として数回の読み取りになります。

## 検証内容（v2.6）

模擬 Firestore を作り直しました。本物と同じように、次のことを再現しています。
- `_updatedAt` による絞り込み
- サーバー時刻
- 監視ごとの変更差分（追加・変更・削除）
- 端末内キャッシュ
- オフライン中は送信が完了しない
- 読み取り回数の数え方

この環境で、次の **20項目がすべて通りました。**

- **読み取り量：**
  - 3,006件で、他の端末の3件の変更 → 2回目の起動の読み取りは **3回**。
  - 変更がなければ **0回**。
- **移行：**
  - 初回は全件を読み、旧形式のデータをすべて新形式に書き直す。
  - 端末内のデータに同期用の欄は混ざらない（JSON 書き出しも従来どおり）。
- **削除：** 削除済みの印がもう1台に届き、復活しない。
- **変更の送信と反映：**
  - 閉じている間のローカルの変更（削除・編集・追加）を起動時に送る。
  - 同じ項目の同時編集では、未送信のローカルの編集を優先する。
  - 他の端末の変更（追加・変更・削除）が、使用中にリアルタイムで届く。
  - カテゴリーの並べ替えが他の端末に反映される。
- **取り消し・復元：**
  - 削除の取り消し（Undo）で、クラウドにも元に戻る。
  - JSON の復元（全置き換え）で、消えた項目には削除済みの印が付き、残りは送信される。
- **全件読み込み：**
  - 30日たつと全件を読み直す。
  - 旧版の端末がクラウドから完全に消した項目も、そのとき反映する。
  - 別アカウントのログインでは全件モードになり、データが混ざらない。
- **オフライン：** キャッシュで開始し、再接続で差分を受け取る。その後で定期支出を判断するので、他の端末で削除した定期支出の記録が復活しない。
- **回帰：** #1（注入対策）、#2（自動復帰 ×3）、#4（authDomain）、未ログイン時の定期支出。

**本物の Firebase には接続していません。** 上の更新手順のとおり、1台目で同期を確認してから2台目を開いてください。
翌日以降、Firebase コンソールの「使用量」で読み取り回数を確認してください。

## ⏸ 注意・未対応

| 内容 | 説明 |
|---|---|
| 削除済みの印は残り続ける | 1件あたり数十バイトの小さなドキュメントです。容量・料金への影響はほぼありません（30日ごとの全件読み込みでは読み取りに含まれます）。 |
| 旧版（v2.5 以前）の端末を使い続けた場合 | 旧版で入力・削除した内容は、他の端末に最大30日間届きません（30日ごとの全件読み込みで反映されます）。必ず全端末を更新してください。 |

---

# 〔参考〕v2.5 修正内容

点検レポートの指摘 **#3（Firestore の読み取り量）** に対応しました。v2.4 の修正（#1・#2・#4・#5）はすべて含んでいます。

## ⚠️ アップロード時の注意

- 置き換えるのは **`app.js` と `sw.js` の2ファイルだけ**です（v2.3 から更新する場合も同じ2ファイルです）。
- **Firestore のデータ構造・セキュリティルールは変更不要です。** v2.3・v2.4 の端末と混在しても同期できます。
  - ただし、読み取りが減るのは v2.5 の端末だけです。
- v2.4 の #4（ログイン対策）の設定手順は、下の v2.4 の節を見てください。

## ✅ 対応内容

| # | 内容 | 修正方法 |
|---|---|---|
| 3 | 起動のたびに全件を2回読んでいた | 起動時の全件読み込み（`get()`）をやめました。リアルタイム監視（`onSnapshot`）の最初の結果を、初期化の統合処理にそのまま使います。<br>・**起動時の読み取りが約半分**になります（模擬環境で 1,006件 → v2.4: 2,012回、v2.5: 1,006回）。<br>・Firebase の料金仕様では、監視は前回の接続から30分以内に再開すると、変わったドキュメントの分だけ課金されます。短い間隔で開き直す使い方では、さらに減る見込みです（本番の課金画面で確認してください）。 |
| 3 | 初期化の待ち方 | 端末内のコピー（キャッシュ）が先に届いても、**サーバーの結果がそろうまで待ってから**統合します。削除の判定（他の端末で削除した記録を復活させない仕組み）は従来どおり働きます。<br>・電波がないときは、すぐに端末内のコピーで始めます。<br>・電波があるのにサーバーが10秒応答しないときも、端末内のコピーで始めます（従来の `get()` と同じ扱い）。<br>・どちらの場合も、削除の判定と定期支出の自動追加は、サーバーの結果が届くまで行いません（v2.4 の #5 と同じ）。 |
| — | 初期化中に届いた変更 | 初期化の途中で届いた他の端末の変更や自分の送信の結果は、初期化の後に反映します。取りこぼしはありません。 |
| — | オフライン起動で、同期の初期化が終わらないことがある（v2.4 以前から） | オフライン中に端末だけの記録があると、初期化がその送信の完了（＝電波の回復）を待ったまま止まり、その間の編集が同期の対象になっていませんでした。<br>オフラインで始めたときは送信の完了を待たずに使い始めるようにしました。送信は電波の回復後に行われ、失敗したら v2.4 の自動復帰で送り直します。 |

### さらに減らすには（今回は未対応）

v2.5 でも、30分以上あけて開くたびに全件を1回読みます。
- 例：記録3,000件・1日10回・2台なら、1日あたり約6万回です（v2.4 は約12万回）。
- 無料枠（5万回/日）に収めるには、「前回以降に変わった分だけ読む」方式が必要です。
  - 削除を「削除済みの印」で記録するなど、データの形式が変わります。
  - そのため、全端末の同時更新が必要です。
- 必要になったら対応します。

## 検証内容（v2.5）

模擬 Firebase を、本物の Firestore（永続キャッシュあり）に近い動きに改良しました。
- 監視の開始時に、まず端末内のコピー、次にサーバーの結果を届けます。
- オフライン中の送信は、電波が戻るまで完了しません。

この環境で、v2.4 の19項目に #3 の5項目を加えた **24項目がすべて通りました**。

- **#3：**
  - 1,006件で、起動時の読み取りが 2,012回 → 1,006回になる。
  - キャッシュが先に届いても、他の端末で削除した記録を復活させない。
  - サーバーが応答しないときは10秒後にキャッシュで始め、定期支出は保留する。
  - 初期化中に届いた変更を取りこぼさない。
  - オフラインで端末だけの記録があっても、初期化が止まらない。

**本物の Firebase には接続していません。** 次の手順で確認してください。
1. 更新前に JSON バックアップを取る
2. 1台目で同期を確認する
3. 翌日以降、Firebase コンソールの「使用量」で読み取り回数が減っていることを確認する

---

# 〔参考〕v2.4 修正内容

点検レポート（v2.3・71点）の指摘 **#1・#2・#4・#5** に対応しました。

## ⚠️ アップロード時の注意

- 置き換えるのは **`app.js` と `sw.js` の2ファイルだけ**です。
  - `index.html`、`styles.css`、`tailwind.css`、`vendor/Sortable.min.js`、`manifest.webmanifest`、アイコンは v2.3 のままです。
- **Firestore のデータ構造・セキュリティルールは変更不要です。** v2.3 の端末と混在しても同期できます。
- **#4（ログイン対策）を有効にするには、Google Cloud の設定が1か所必要です**（下の「#4 の設定手順」を参照）。
  - 設定しなくても、v2.3 までと同じ方法でログインできます（悪化はしません）。

## ✅ 対応内容

| # | 内容 | 修正方法 |
|---|---|---|
| 1 | JSON 復元経由のスクリプト注入 | ・履歴の時刻と、Shop タブ「直近の利用」の日付・時刻を、表示時にエスケープするようにしました。<br>・クイック入力の金額は、数値に変換してから表示するようにしました（文字列がそのまま画面に入りません）。<br>・復元時の検証を追加しました。記録の時刻は「空」または `H:MM` / `HH:MM`、登録日時（`ts`）は数値、クイック入力の金額は数値か未指定（null）だけを受け付けます。 |
| 2 | 同期が失敗したまま復帰しない | 次の3つの失敗から自動で復帰します。<br>・起動時の同期の初期化<br>・変更の送信<br>・リアルタイム監視（他の端末の変更の受信）<br>5秒 → 10秒 → 20秒 … と間隔を広げながら（最長5分）再試行します。電波が戻ったときと、アプリを開き直したときは、すぐに再試行します。<br>電波がない間は再試行せず、「保存待機中 (オフライン)」と表示します。<br>送れなかった変更は未送信として残り、復帰後に送られます。ログアウトすると再試行は止まります。 |
| 4 | iPhone のホーム画面版で Google ログインが失敗する（Safari のドメイン分離） | ・Firebase Hosting の既定ドメイン（`expense-tracker-5e542.web.app` / `.firebaseapp.com`）で開いたときは、ログイン処理のドメイン（`authDomain`）をアプリと同じドメインにします。<br>・ホーム画面版（standalone）では、ポップアップではなくリダイレクト方式でログインします（同じドメインにできる場合だけ）。<br>・それ以外の場所で公開している場合は、従来どおり `firebaseapp.com` を使います。 |
| 5 | オフライン起動で、削除した定期支出の記録が復活する | ログイン中は、**すべての同期データをサーバーで確認できるまで、定期支出の自動追加を保留**します。<br>・キャッシュ（端末内の古いコピー）だけで起動した場合は追加しません。電波が戻り、最新の状態を受け取ってから判断します。<br>・保留中は「保存待機中 (オフライン)・定期支出は接続後に追加します」と表示します。<br>・ログインしていない場合は、従来どおりすぐに追加します。 |
| — | JSON バックアップの版表記 | `version` を `"2.1"` から `"2.4"` に更新しました。 |

### 動作の変化（知っておいてほしいこと）

- **ログイン中に電波のない場所でアプリを開くと、その日の定期支出はすぐには追加されません。** 電波が戻った時点で追加されます（二重に追加されることはありません）。
- 電波のない場所では、同期の表示が「同期中…」のまま止まらず、「保存待機中 (オフライン)」になります。
- 復帰の確認のため、リアルタイム監視が「サーバーで確認できたか」の通知も受け取るようにしました（`includeMetadataChanges`）。データが変わっていない通知では画面を再描画しません。この通知で Firestore の読み取り回数は増えません。

## #4 の設定手順（Firebase Hosting で公開している場合）

`https://expense-tracker-5e542.web.app/` で公開している場合の手順です。

1. **Google Cloud Console** → 「API とサービス」→「認証情報」を開きます。
2. 「OAuth 2.0 クライアント ID」の **Web client (auto created by Google Service)** を開きます。
3. 「承認済みのリダイレクト URI」に次を追加して保存します。
   `https://expense-tracker-5e542.web.app/__/auth/handler`
4. **Firebase コンソール** →「Authentication」→「設定」→「承認済みドメイン」に `expense-tracker-5e542.web.app` があることを確認します（通常は最初から入っています）。
5. 反映まで数分かかることがあります。iPhone のホーム画面版で、ログアウト → ログインを試してください。

**独自ドメインを Firebase Hosting につないでいる場合：**
- `app.js` の `SAME_ORIGIN_AUTH_HOSTS` にそのドメインを追加します（コメントの例を参照）。
- 手順3・4にも、そのドメインを追加します。

**Firebase Hosting 以外（GitHub Pages など）で公開している場合：**
- コードは従来どおり `firebaseapp.com` を使うため、この問題は残ります。
- 解決するには、Firebase Hosting への移行か、`/__/auth/` をリバースプロキシで転送する設定が必要です。

## 検証内容（v2.4）

模擬 Firebase（jsdom 上で `app.js` を実際に起動）で、次の19項目を確認しました。
同じテストを v2.3 に当てると、対応した不具合のうち12項目が再現し、v2.4 ではすべて期待どおりに動きました。

- **#1：** 履歴の時刻・クイック入力の金額に HTML を仕込んでも、実行されずに文字として表示される。不正な時刻・金額の JSON は復元を拒否し、正常な JSON は復元できる。
- **#2：** 初期化の失敗から約5秒で自動復帰する。オフライン中は待機し、`online` で即再試行する。送信の失敗から自動で送り直す。ログアウトで再試行が止まる。
- **#5：** キャッシュだけの起動では定期支出を追加しない。再接続後に、他の端末で処理済みの定期支出を再生成しない（サーバーへの復活もない）。データ変化のない再接続でも保留が解除される。オンライン起動・未ログインでは従来どおり追加する。
- **#4：** web.app で開くと `authDomain` が同じドメインになる。GitHub Pages などでは従来の値のまま。ホーム画面版ではリダイレクト、通常のブラウザではポップアップでログインする。
- **回帰：** 他の端末の更新がリアルタイムに反映される。

テストの途中で、「サーバー確認済み」の印を付ける順序の不具合を1件見つけて直しました（データを反映する前に確認済みにすると、古い定期支出のまま自動追加が走っていました）。

**本物の Firebase には接続していません。** 更新前に JSON バックアップを取り、1台目で同期とログインを確認してから2台目を更新してください。

## ⏸ 引き続き未対応

| 内容 | 理由 |
|---|---|
| 点検 #3：Firestore の読み取り量（起動のたびの全件 `get()`） | 今回の対象外です。 |
| 点検 #6〜#11（編集時の支払い方法の自動更新、ズーム禁止など） | 今回の対象外です。 |
| ボタンの固定処理の onclick の全廃・コード分割 | v2.3 と同じ理由で見送りました。 |
| 古い保存キー（v15〜v17）の削除 | 万一のときの復旧元になるため、残しています。 |

---

# 〔参考〕v2.3 修正内容

## ⚠️ アップロード時の注意

- 次の5ファイルを置き換えてください：`index.html`、`app.js`、`styles.css`、`tailwind.css`、`sw.js`。
  - `vendor/Sortable.min.js` と `manifest.webmanifest` は v2.2 から変わっていません。
- **サーバー（Firestore）の設定変更は不要です。** 取引・定期支出・クイック入力に `shopId` という欄が増えるだけです。
- 更新後に初めて起動すると、既存の記録に `shopId` が自動で付きます。
  - 付いた結果は通常の同期でクラウドへ送られます。
  - 件数が多いと、初回だけ同期に数秒かかります。
- **iPhone・Mac の両方を v2.3 に揃えてください。**
  - v2.2 の端末で記録を編集しても壊れません。v2.3 の端末が次に起動したときに、自動で `shopId` を付け直します。

## ✅ 追加・修正

| 内容 | 詳細 |
|---|---|
| お店を `shopId` で参照（点検 #9） | 記録・定期支出・クイック入力が、お店をIDで参照するようにしました。<br>・`shopName` も残すため、表示と互換性はそのままです。<br>・既存データは「店名＋分類」から自動でIDを付けます。<br>・同じ名前のお店が複数あり、分類でも絞れない記録は、従来どおり名前だけで扱います。 |
| お店の名前変更 | IDで紐付いた記録は、名前を変えても確実に追従します。 |
| 同じ名前のお店を区別 | 分析の Shop タブで、分類の違う同名のお店を別々に集計します。<br>Monthly・Yearly の「お店別」では、名前が重なるときだけ「駅前店（カフェ）」のように分類名を添えます。 |
| 削除したお店の記録 | 記録時の名前とIDを保ったまま残ります。 |
| 支払い方法の自動更新 | 「最後に使った支払い方法を既定にする」も、お店IDで照合するようにしました。 |
| **削除の取り消し（Undo）** | 削除の直後、画面下に「〇〇を削除しました ／ 元に戻す」を約7秒表示します。<br>・対象：記録、定期支出、お店、分類、カテゴリー、支払い方法、クイック入力、時間帯、クイック時刻<br>・元に戻すと、同じIDで元の位置に戻ります。クラウドにも再保存されます。<br>・削除前の確認ダイアログは従来どおり出ます。 |
| 直近7日の1日あたり（点検 #16） | Trend の「今週の1日あたり」を「直近7日の1日あたり」に変えました。比較対象は、その前の8週間です。<br>これで、日曜に1日分だけで計算されて数字が大きく揺れることがなくなります。<br>支出の推移グラフ（週ごと）は従来どおり、日曜始まりの週です。 |
| 金額未定の注記（点検 #17） | 金額未定の記録が期間内にあるとき、赤字で「※ 金額未定の記録 N件 は含まれていません」と表示します。<br>表示場所：Monthly・Yearly の合計の下、Trend の着地予測の下 |

## ⏸ 引き続き未対応

| 内容 | 理由 |
|---|---|
| `authDomain` の Safari 対策（点検 #20） | アプリをどこで公開しているか（Firebase Hosting か、それ以外か）で手順が変わります。コードだけでは対応できません。<br>公開先を教えてもらえれば手順を案内します。 |
| ボタンの固定処理の onclick の全廃・コード分割 | データを埋め込む危険な箇所は v2.2 で対応済みです。残りは固定の処理だけで、安全上の問題はありません。<br>大規模に書き換えると挙動が変わる恐れがあるため、見送りました。 |
| 古い保存キー（v15〜v17）の削除 | 万一のときの復旧元になるため、残しています。 |

## 検証内容（v2.3）

- **見た目：** 12画面を v2.2 以前と比較しました。変わったのは次の2か所だけでした。
  - 月送りの修正
  - Trend の「直近7日」の表示
  - （削除時に出る「元に戻す」の表示は新しく追加したものです）
- **同期：** 模擬 Firestore の8つのシナリオをすべて再実行し、v2.2 と同じ結果になりました。
- **新機能：** 次の動作を確認しました。
  - 既存データへの `shopId` 自動付与：同名店は分類で区別し、閉店した店は名前のまま残る
  - Shop タブでの同名店の別集計
  - 名前変更の追従
  - 記録削除の取り消しで、元の位置に戻ること
  - 分類削除の取り消し
  - トーストが7秒後に消えること
  - 金額未定の注記

---

# 〔参考〕v2.2（点検修正版）修正内容

対象：点検レポートの指摘事項のうち、下記「✅ 修正済み」のもの。
デザイン・画面構成・操作手順は変更していません（修正前後の画面をピクセル単位で比較し、変わったのは月送りバグの修正箇所だけでした）。

## ⚠️ アップロード時の注意

- **ファイル一式をすべて置き換えてください。**
  - 既存：`index.html` `app.js` `styles.css`
  - 新規：`tailwind.css` `vendor/Sortable.min.js` `sw.js` `manifest.webmanifest`
- `favicon-32.png` と `apple-touch-icon.png` は、今あるものをそのまま使います。
- **Firestore のデータ構造・コレクション名・セキュリティルールは変更不要です。**
  旧版と新版の端末が同じアカウントで混在しても同期できます。
  ただし、削除の復活防止などの効果は新版の端末にだけ働くため、全端末の更新をおすすめします。
- 更新後に初めて開いたときは、以前と同じ動作（和集合マージ）で1回同期します。
  その時点から「同期済みIDの記録」が始まり、2回目以降は削除した記録が復活しなくなります。

## ✅ 修正済み

### 1. 同期の安全化（データ消失・混入対策）

| # | 内容 | 修正方法 |
|---|---|---|
| 1 | 他の端末で削除した記録が復活する | 端末ごとに「クラウドに存在を確認したID」を記録します（端末内のみ、サーバーには保存しません）。以前クラウドにあって今は無い項目は削除済みと判断し、再アップロードしません。オフライン起動時やクラウドが丸ごと空のときは、安全側に倒して削除判定をしません。 |
| 1 | 定期支出が他端末で再生成される | 最終実行日を各定期支出の `lastRunDate` 欄にも保存します（既存の fixedExpenses コレクションの中の欄なので、ルール変更は不要です）。また、自動追加はクラウドの最新状態を読み込んでから実行します。 |
| 2 | 別アカウントにデータが混入する | 前回と違うアカウントでログインした場合、前のアカウントで同期済みのデータは持ち込みません。ログアウト中に新しく入力した分だけを引き継ぎます。新しいアカウントで設定が空のときは初期設定を入れます。 |
| 3 | 500件を超えると同期が失敗する | 400件ずつに分割して送信します（1,200件でテスト済み）。 |
| 4 | 送信待ちの編集が、他端末からの更新で消える | 未送信の変更がある間に更新が届いた場合は、自分が触った項目はローカルの内容、触っていない項目はリモートの内容を採用して統合します。 |
| — | ログアウト後もリアルタイム監視が残る | ログアウト時に監視を解除し、同期状態をリセットします。二重ログイン時の二重登録も防止しました。 |
| — | `enablePersistence` の失敗を捕まえられていない | Promise の `.catch` で処理するようにしました。 |
| — | 復元直後のリロードで送信が途中で切れる | クラウドへの送信完了を待ってから再読み込みします。 |
| — | アプリを閉じる直前の変更が送られない | 画面が裏に回るときに、未送信の変更をすぐ送ります。 |

### 2. ロジックの不具合

| # | 内容 |
|---|---|
| 7 | 月送り・年送りで月が飛ぶ（8/31→10/1）。月末日に丸めるようにしました（履歴・分析の両方）。 |
| 8 | 既存の記録を編集中に日付を変えても、時刻が消えないようにしました。新規入力時の「時間を選び直す」動作は従来どおりです。 |
| 9 | お店の名前を変えると、過去の記録・定期支出・クイック入力のお店名も追従するようにしました。同じ名前のお店が他の分類にある場合は、分類も一致するものだけを変えます。「最後に使った支払い方法を既定にする」も分類まで照合します。 |
| 10 | 分類を削除すると、所属していたお店がお店リストの「未分類」欄に表示されるようにしました。そこから編集・削除したり、別の分類へドラッグしたりできます。使用中のカテゴリー・支払い方法を削除するときは、影響件数を表示します。 |
| 11 | 定期支出を「毎月の追加日」と同じ日に登録すると、今日の分も追加されるようにしました。アプリを開いたまま日付をまたいだ場合も処理されます。 |
| 12 | 一覧の⊖ボタンから時間帯・クイック時刻を最後の1件まで消せないようにしました。 |
| 13 | CSV の修正です。時間未設定の記録を「00:00」と出力しないようにしました。ファイル名は日本時間の日付にしました。末尾に「費目タイプ」（固定費/変動費）列を追加しました。 |
| 14 | 日付文字列をローカル日付として解釈するようにしました。海外のタイムゾーンでも曜日や週がずれません。 |
| — | 「金額だけ保存」の後、履歴画面を必ず今日の日付で表示するようにしました。 |

### 3. 分析

| # | 内容 |
|---|---|
| 15 | 「今月の着地予測」と「カテゴリー別の変化」で、固定費を日割りで引き伸ばさないようにしました。固定費は実額で計上し、今月まだ追加されていない定期支出は予定額として加えます。日割りで伸ばすのは変動費だけです。月初に家賃が入っても、予測が数百万円に跳ね上がりません。 |

### 4. セキュリティ・オフライン

| # | 内容 |
|---|---|
| 18 | 「すべて見る」ボタンにデータを埋め込むのをやめました。`McDonald's` のように `'` を含む店名でも動作します。 |
| 19 | 設定一覧（お店・分類・カテゴリー・支払い方法・時間帯・クイック入力）で、IDを onclick 文字列に埋め込まず、イベントリスナーで処理するようにしました。時刻や日数も表示時にエスケープします。 |
| 19 | JSON 復元時の検証を強化しました。全項目のID（Firestore で使えない `/` などの禁止）、IDの重複、日付・時刻・金額の形式を確認します。 |
| 21 | CSV で `= + - @` から始まる文字の先頭に `'` を付け、Excel で数式として実行されないようにしました。 |
| 6 | Tailwind を CDN の実行時生成から、同じ v3.4.1 でビルドした `tailwind.css` に変更しました（見た目は同一）。SortableJS も同梱しました。並び替えライブラリが読めない場合でも一覧は表示されます。 |
| 5/6 | Service Worker と manifest を追加しました。一度オンラインで開けば、電波の無い場所でも起動できます。アプリ本体は「ネット優先」で取得するため、ファイルを更新すれば次回起動時に反映されます。Firestore とログインの通信には関与しません。**iPhone では「ホーム画面に追加」して使うと、Safari の7日間データ自動削除の対象外になります。** |

### 5. UX・アクセシビリティ

- 詳細確認画面の「保存する」ボタンの下余白を、iPhone のホームインジケータ領域以上に確保するようにしました。ホームインジケータの無い機種では従来と同じ30pxです。
- アイコンだけのボタンに読み上げ用ラベル（`aria-label`）を付けました。

## ⏸ 今回は未対応（理由つき）

| # | 内容 | 理由 |
|---|---|---|
| 9 | お店を `shopId` で参照する方式への移行 | 旧版の端末と同期データの形式が変わるため見送りました。代わりに「名前変更の追従」で実害を解消しています。 |
| 16 | 今週の1日あたり（日曜は1日分だけで計算）の不安定さ | 表示の意味が変わる仕様変更になるため、見送りました。 |
| 17 | 金額未定の記録が含まれる期間の注記 | 画面デザインの追加が必要なため、見送りました。 |
| 20 | `authDomain` の Safari 対策 | Firebase Hosting 側の設定変更が必要なため、コードだけでは対応できません。 |
| — | 静的な onclick（ボタンの固定処理）の全廃・コード分割・テスト整備 | 挙動を変えずに大規模に書き換えることになるため、見送りました。データが埋め込まれる危険な箇所は修正済みです。 |
| — | 削除の取り消し（Undo） | 新機能の追加になるため、見送りました。 |
| — | 入力欄の自動ズーム・`user-scalable=no` | **点検レポートの訂正です。** 現在の `maximum-scale=1` が iOS のフォーカス時自動ズームを防いでいるため、変えるとかえってズームが起きます。現状維持にしました。 |
| — | 古い保存キー（v15〜v17）の削除 | 万一のときの復旧元になるため、残しています。 |

## 検証内容

- **見た目**：修正前後の12画面をピクセル比較しました（iPhone 相当の 390×844）。
  - 変わったのは、月送りバグの修正箇所の1か所だけでした。
- **同期**：模擬 Firestore で8つのシナリオを検証しました。
  - 対象：削除の復活、アカウント切替、1,200件、送信待ち中の競合、オフライン起動、定期支出の再生成、ログアウト後の監視、通常の編集・削除。
  - 旧版ではシナリオ1・2・3・4・6・7で不具合が再現し、新版ではすべて期待どおりに動きました。
- **画面操作**：以下が正しく動くことを確認しました。
  - 「すべて見る」（`'` を含む店名）
  - 日付変更後の時刻保持
  - 店名変更の追従
  - 分類削除後の未分類表示
  - 最低1件のチェック
  - 当日登録の定期支出
  - CSV 出力
  - 1/31→2月の月送り
- **本物の Firebase には接続していません。** 実機では、次の順で確認することをおすすめします。
  1. 更新前に JSON バックアップを取る
  2. 1台目を更新して同期を確認する
  3. 2台目を更新する
